This folder contains compiled binaries for the GLEW 1.11.0 library.

Note: on Mac build against i386 and x86_64 architectures by editing make files. Also changed makefiles to use LIBTOOL instead of AR
To build:
make STRIP=

Original source code can be found here:
http://glew.sourceforge.net/